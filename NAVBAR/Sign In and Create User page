<!doctype html>
<html lang="en" data-theme="dark">
<head>
  <meta charset="utf-8" />
  <title>Prep Penguin — Account</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <style>
    :root{
      /* DARK THEME (default) */
      --bg: #0b0b0c;
      --bg-2: #161618;
      --panel: #121214;
      --text: #ffffff;
      --muted: #cfcfd5;
      --input: #101013;
      --input-border: #2b2b30;
      --outline: #222;
      --accent: #ff7a00;
      --accent-ink: #0b0b0c;
      --link: #ffb380;
      --shadow: 0 10px 30px rgba(0,0,0,.40);
      --btn-shadow: 0 8px 24px rgba(255,122,0,.22);
    }
    [data-theme="light"]{
      /* LIGHT THEME */
      --bg: #fafafa;
      --bg-2: #ffffff;
      --panel: #ffffff;
      --text: #0b0b0c;
      --muted: #5a5a62;
      --input: #ffffff;
      --input-border: #dcdce3;
      --outline: #e9e9ef;
      --accent: #ff7a00;
      --accent-ink: #0b0b0c;
      --link: #cc4e00;
      --shadow: 0 10px 30px rgba(0,0,0,.08);
      --btn-shadow: 0 8px 24px rgba(255,122,0,.22);
    }

    html,body{height:100%}
    body{
      margin:0; font-family: system-ui,-apple-system,Segoe UI,Roboto,Inter,Arial;
      background: linear-gradient(180deg,var(--bg) 0%, var(--bg-2) 100%);
      color:var(--text); display:grid; place-items:center;
    }
    .card{
      width:min(520px,92vw); background:var(--panel); border:1px solid var(--outline);
      border-radius:20px; padding:28px; box-shadow:var(--shadow); position:relative;
    }
    h2{ margin:0 0 4px; font-size:26px }
    .muted{ color:var(--muted); margin:0 0 18px }
    form{ display:grid; gap:12px; margin-top:10px }
    label{ font-size:13px; color:var(--muted) }
    input{
      width:100%; padding:12px 14px; border-radius:12px; border:1px solid var(--input-border);
      background:var(--input); color:var(--text);
    }
    .btn{
      appearance:none; border:0; padding:12px 16px; border-radius:12px;
      background:var(--accent); color:var(--accent-ink); font-weight:700; cursor:pointer;
      box-shadow:var(--btn-shadow); transition:filter .15s ease
    }
    .btn:hover{ filter:brightness(1.05) }
    .alt{ font-size:13px; color:var(--muted); text-align:center; margin-top:8px }
    .link{ color:var(--link); cursor:pointer }
    .logo{ display:flex; align-items:center; gap:12px; margin-bottom:16px }
    .logo img{ width:48px; height:48px; object-fit:contain }  /* bigger */
    .logo strong{ font-size:16px }
    .row-between{ display:flex; justify-content:space-between; align-items:center }

    /* Theme switch */
    .theme-toggle{
      display:inline-flex; align-items:center; gap:8px; font-size:12px; color:var(--muted);
      background:transparent; border:1px solid var(--outline); padding:6px 10px; border-radius:999px; cursor:pointer;
    }
  </style>
</head>
<body>
  <div class="card">
    <div class="row-between">
      <div class="logo">
        <!-- Use your transparent PNG below -->
        <a href="index.html" style="display:flex; align-items:center; gap:12px; text-decoration:none; color:inherit">
          <img alt="Prep Penguin Logo" src="Penguin LOGO-modified.png" />
          <strong>Prep Penguin</strong>
        </a>
      </div>
      <button id="themeToggle" class="theme-toggle" type="button">🌙 Dark</button>
    </div>

    <!-- Sign In -->
    <section id="pane-signin">
      <h2>Welcome back</h2>
      <p class="muted">Log into your account to continue.</p>
      <form id="form-signin">
        <div>
          <label>Email</label>
          <input type="email" name="email" required />
        </div>
        <div>
          <label>Password</label>
          <input type="password" name="password" required minlength="6" />
        </div>
        <button class="btn" type="submit">Sign In</button>
      </form>
      <p class="alt">New here? <span class="link" onclick="switchMode('signup')">Create an account</span></p>
    </section>

    <!-- Sign Up -->
    <section id="pane-signup" hidden>
      <h2>Create your account</h2>
      <p class="muted">Start prepping in minutes.</p>
      <form id="form-signup">
        <div>
          <label>Full name</label>
          <input type="text" name="name" required />
        </div>
        <div>
          <label>Email</label>
          <input type="email" name="email" required />
        </div>
        <div>
          <label>Password (min 6 chars)</label>
          <input type="password" name="password" required minlength="6" />
        </div>
        <button class="btn" type="submit">Create Account</button>
      </form>
      <p class="alt">Already have an account? <span class="link" onclick="switchMode('signin')">Sign in</span></p>
    </section>
  </div>

  <script>
    // --- Mode switch (signin/signup) ---
    const paneIn = document.getElementById('pane-signin');
    const paneUp = document.getElementById('pane-signup');
    function switchMode(mode){
      const isUp = mode === 'signup';
      paneUp.hidden = !isUp;
      paneIn.hidden = isUp;
      history.replaceState({}, '', `?mode=${mode}`);
    }
    const params = new URLSearchParams(location.search);
    switchMode(params.get('mode') === 'signup' ? 'signup' : 'signin');

    // --- Theme toggle with persistence ---
    const root = document.documentElement;
    const toggle = document.getElementById('themeToggle');
    const saved = localStorage.getItem('pp-theme');
    if (saved) root.setAttribute('data-theme', saved);
    updateToggleLabel();

    toggle.addEventListener('click', ()=>{
      const current = root.getAttribute('data-theme') || 'dark';
      const next = current === 'dark' ? 'light' : 'dark';
      root.setAttribute('data-theme', next);
      localStorage.setItem('pp-theme', next);
      updateToggleLabel();
    });

    function updateToggleLabel(){
      const theme = root.getAttribute('data-theme') || 'dark';
      toggle.textContent = theme === 'dark' ? '🌙 Dark' : '☀️ Light';
    }

    // --- Placeholder handlers (replace with real auth later) ---
    document.getElementById('form-signin').addEventListener('submit', (e)=>{
      e.preventDefault();
      const data = Object.fromEntries(new FormData(e.target));
      alert(`Sign In\nEmail: ${data.email}`);
    });
    document.getElementById('form-signup').addEventListener('submit', (e)=>{
      e.preventDefault();
      const data = Object.fromEntries(new FormData(e.target));
      alert(`Sign Up\nName: ${data.name}\nEmail: ${data.email}`);
    });
  </script>
</body>
</html>
